# TODO(implementation phase): expose once module "alb" above is wired in.
#
# output "alb_arn"            { value = module.alb.arn }
# output "alb_dns_name"       { value = module.alb.dns_name }
# output "target_group_arn"   { value = module.alb.target_groups["app"].arn }
# output "security_group_id"  { value = module.alb.security_group_id }

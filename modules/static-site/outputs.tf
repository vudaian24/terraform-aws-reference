# TODO(implementation phase): expose once modules above are wired in.
#
# output "bucket_id"                { value = module.bucket.s3_bucket_id }
# output "bucket_arn"                { value = module.bucket.s3_bucket_arn }
# output "distribution_id"           { value = module.cdn.cloudfront_distribution_id }
# output "distribution_domain_name"  { value = module.cdn.cloudfront_distribution_domain_name }
